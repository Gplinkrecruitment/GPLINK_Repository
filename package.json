{
  "name": "gp-link-app",
  "version": "1.0.0",
  "private": true,
  "description": "GP Link app with OTP auth",
  "scripts": {
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "init:db": "node scripts/init-db.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "agents": "node scripts/agents.js",
    "gplink": "node scripts/agents.js",
    "gplink:memory": "node scripts/agent-memory.js",
    "agent-bridge": "node scripts/agent-bridge.js"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "googleapis": "^171.4.0",
    "mammoth": "^1.12.0",
    "pdf-lib": "^1.17.1",
    "pdfkit": "^0.16.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
